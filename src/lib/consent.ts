/**
 * Current versions of the legal documents users must consent to. Bump these
 * whenever the corresponding document at /terms or /privacy changes in a way
 * that requires re-acceptance — a user whose latest acceptance predates the
 * current version is treated as not having consented and is re-prompted.
 */
export const CURRENT_TERMS_VERSION = '2026-07-10';
export const CURRENT_PRIVACY_VERSION = '2026-07-10';
