SELECT 
  pid,
  usename,
  application_name,
  client_addr,
  state,
  query
FROM pg_stat_activity
WHERE state != idle;  postgresql://postgres@localhost:5432/mruhacks_dev
SELECT 
  pid,
  usename,
  application_name,
  client_addr,
  state,
  query
FROM pg_stat_activity
WHERE state != idle;  postgresql://postgres@localhost:5432/mruhacks_dev
